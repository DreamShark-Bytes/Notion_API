[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notion-api"
version = "1.3.0"
description = "Shared Notion API client and data extraction utilities"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[tool.setuptools]
py-modules = ["notion_api"]
